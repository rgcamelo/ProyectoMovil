<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyecto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity android:name=".Controller.MunicipalityDetailActivity"
            android:label="Chimichagua"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".Controller.ResultsSearchActivity"
            android:label="Resultados"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Controller.InterestSearchFragmentActivity"
            android:label="Interes"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Controller.WhereSearchFragmentActivity"
            android:label="Donde"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Controller.CalendarWhenSearch"
            android:label="Selecciona una fecha"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Controller.WhenSearchFragmentActivity"
            android:label="Cuando"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Controller.DetailActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Controller.ContainerActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>